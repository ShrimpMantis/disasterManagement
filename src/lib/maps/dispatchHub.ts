import type { MapLatLng } from "@/types/map";

/** Default map origin for dispatch routing visualizations. */
export const CENTRAL_DISPATCH_HUB: MapLatLng & { label: string } = {
  label: "District Central Warehouse",
  lat: 26.75,
  lng: 94.22,
};
